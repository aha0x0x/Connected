This coding challenge determines if two cities are connected given a list of cities in a file. 
The solution uses a custom stream collector to create collect the data from the file. 

compile: `mvn compile`
run unit tests: `mvn test`
package: `mvn package`  


To run after packaging:
 `java -jar target/Connected-1.0.jar <filename> <city1> <city2>`
 eg: `java -jar target/Connected-1.0.jar cities.txt Montreal Toronto` 
 
